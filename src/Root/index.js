import MainTemplate from "../components/templates/MainTemplate";
import Router from "../routing/Router";

const Root = () => (
  <>
    <MainTemplate>
      <Router />
    </MainTemplate>
  </>
);

export default Root;
